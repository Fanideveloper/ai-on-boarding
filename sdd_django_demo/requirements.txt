# Resolved 2026-08-19 against Python 3.14 (Django 6.1 requires Python >=3.12; this file works
# on any 3.12+ interpreter, this is just what was actually installed and tested).
# psycopg added 2026-09-02, pinned to the current stable release, for Postgres in every
# environment - see settings.py's Database section.
# gunicorn added 2026-09-02, pinned to the current stable release - the WSGI server a real
# deployment runs behind (manage.py runserver is dev-only and isn't meant for production
# traffic); unused by local dev or the test suite.
# requests added 2026-09-10, pinned to the current stable release, for the outbound call
# that asks Google to verify an access token (api/google_auth.py); certifi,
# charset-normalizer, idna and urllib3 come with it.
# python-dotenv added 2026-09-10, pinned to the current stable release, so the Google
# settings can be read from a local .env instead of being exported by hand on every start.
asgiref==3.12.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
Django==6.1
djangorestframework==3.18.0
drf-spectacular==0.30.0
gunicorn==26.2.0
idna==3.19
inflection==0.5.1
iniconfig==2.3.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
packaging==26.3
pluggy==1.6.0
psycopg[binary]==3.3.5
Pygments==2.21.0
pytest==9.1.1
pytest-django==4.14.0
python-dotenv==1.2.3
PyYAML==6.0.3
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
sqlparse==0.6.0
uritemplate==4.2.0
urllib3==2.7.0
